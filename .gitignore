# dependencies
node_modules

# foundry build artifacts
packages/foundry/out
packages/foundry/cache
packages/foundry/broadcast

# secrets — never commit env files (Anthropic key, deployer private key)
.env
.env.local
.env*.local
**/.env
**/.env.local
**/.env*.local

# agent-call observability sink (written at <cwd>/.agent-calls.jsonl if AGENT_CALL_LOG is unset)
.agent-calls.jsonl
**/.agent-calls.jsonl

# yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# eslint
.eslintcache

# misc
.DS_Store

# IDE
.vscode
.idea

# cli
dist
